{
  "name": "elroy-app",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start:web": "yarn workspace @elroy/web start",
    "start:mobile": "yarn workspace @elroy/mobile start",
    "build:web": "yarn workspace @elroy/web build",
    "build:mobile": "yarn workspace @elroy/mobile build",
    "test": "yarn workspaces run test"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
