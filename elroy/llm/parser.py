from typing import TypeVar

T = TypeVar("T")

# TODO: Write an abstract parser class that can handle a stream of either StreamItem or str (just use a generic type),
# and three things: Main chat content (str), internal thought (str), and list of FunctionCall.
