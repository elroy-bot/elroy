def throw_error() -> None:
    """Testing function designed to validate proper error handling. Should only be called if prompted to do so by an admin."""
    raise Exception("This is an error. This function is designed to throw an error.")
